name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build
        run: |
          LDFLAGS="-s -w"
          GOOS=linux GOARCH=amd64  go build -ldflags "$LDFLAGS" -o orktree-linux-amd64  ./cmd/orktree
          GOOS=linux GOARCH=arm64  go build -ldflags "$LDFLAGS" -o orktree-linux-arm64  ./cmd/orktree

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            orktree-linux-amd64
            orktree-linux-arm64
